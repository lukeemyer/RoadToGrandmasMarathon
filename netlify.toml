[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA fallback — Netlify routes /.netlify/functions/* natively before this rule
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
